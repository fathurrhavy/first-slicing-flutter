import 'package:day_1_bwa/pages/first_splash.dart';
import 'package:flutter/material.dart';

void main() => runApp(SevenDays());

class SevenDays extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      home: FirstSplash(),
    );
  }
}
